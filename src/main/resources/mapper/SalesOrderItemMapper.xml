<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.mapper.SalesOrderItemMapper">
    <select id="selectByOrderId" resultType="SalesOrderItem">
        SELECT i.*, p.name as product_name
        FROM sales_order_item i
        LEFT JOIN product p ON i.product_id = p.id
        WHERE i.order_id = #{orderId}
    </select>

    <insert id="insert">
        INSERT INTO sales_order_item (order_id, product_id, price, quantity, subtotal)
        VALUES (#{orderId}, #{productId}, #{price}, #{quantity}, #{subtotal})
    </insert>

    <delete id="deleteByOrderId">
        DELETE FROM sales_order_item WHERE order_id = #{orderId}
    </delete>
</mapper>
