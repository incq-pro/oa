<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.mapper.DepartmentMapper">
    <select id="selectAll" resultType="Department">
        SELECT * FROM department ORDER BY id
    </select>

    <select id="selectById" resultType="Department">
        SELECT * FROM department WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO department (name, parent_id) VALUES (#{name}, #{parentId})
    </insert>

    <update id="update">
        UPDATE department SET name = #{name}, parent_id = #{parentId} WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM department WHERE id = #{id}
    </delete>
</mapper>
