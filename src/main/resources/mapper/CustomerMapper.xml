<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.mapper.CustomerMapper">
    <select id="selectAll" resultType="Customer">
        SELECT * FROM customer ORDER BY id
    </select>

    <select id="selectById" resultType="Customer">
        SELECT * FROM customer WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO customer (name, contact, phone, address, status)
        VALUES (#{name}, #{contact}, #{phone}, #{address}, #{status})
    </insert>

    <update id="update">
        UPDATE customer SET name=#{name}, contact=#{contact}, phone=#{phone}, address=#{address}, status=#{status}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM customer WHERE id = #{id}
    </delete>
</mapper>
